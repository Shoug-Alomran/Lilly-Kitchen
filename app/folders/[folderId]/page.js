export default function FolderDetailPage({ params }) {
  return (
    <main className="page">
      <h1>Folder Detail</h1>
      <p>Placeholder for folder ID: {params.folderId}</p>
    </main>
  );
}
